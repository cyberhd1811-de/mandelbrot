import type { Component } from 'solid-js';

import './Header.scss';

const Header: Component = function () {
    return (
        <header id='header'>
            <h1>MANDELBROT</h1>
        </header>
    );
};

export default Header;
